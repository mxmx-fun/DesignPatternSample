// Singleton Pattern(单例模式)
// 单例模式是有且只有一个实例，而且是全局的。

// 举个例子，有个场景管理器，从游戏开始到结束，这个管理器都只负责场景切换。
// 像这种类，就适合用单例模式，因为场景管理器，只需要一个实例就可以了。

//总结：
//优点：单例模式可以避免创建多个实例，方便调用。
//缺点：不好维护，因为是全局的，所以很多地方都可以调用，不好维护。并且不注意使用，将会造成内存泄漏。